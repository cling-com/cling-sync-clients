<?xml version="1.0" encoding="utf-8"?><!--
   cling-sync keeps the repository passphrase and the passphrase-encrypted S3
   credential URI on-device, plus local file-hash caches. None of it may leave the
   device. android:allowBackup="false" already disables Auto Backup and key/value
   backup; these rules also exclude everything from Android 12+ cloud backup and
   device-to-device transfer so no app data is ever copied off the device.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
